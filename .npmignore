# Files excluded from npm package (but tracked in git)
# NOTE: .agents/generated/ IS included (pre-compiled for users)

# Development & repo files
Frame\ 2.png
tests/
docs/
*.log
.DS_Store
.env
.env.*

# CI / GitHub Actions
.github/workflows/
.github/copilot-instructions.md

# Lint configs (not needed at runtime)
.markdownlint.json

# Generated project-root outputs (users generate these themselves)
.cursorrules
CONVENTIONS.md
.aider.conf.yml

# Plugin lock file (per-project, not distributed)
.agents/plugins.json
.agents/plugins/
